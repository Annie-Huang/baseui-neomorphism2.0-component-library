import { Progress } from '@base-ui/react';

const NeuProgress = () => {
  return (
    <Progress.Root>
      <Progress.Track>
        <Progress.Indicator />
      </Progress.Track>
    </Progress.Root>
  );
};

export default NeuProgress;
